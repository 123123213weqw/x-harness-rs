# #57 / #59 / #61 验收记录（2026-09-12）

## 实现范围

- #61：共享 Assistant 投影，分离思考/正文/工具参数的 Web 块编号；完成消息保留
  reasoning，中断消息保留 interrupted；不改模型输入、不增加日志副本。
- #57：两条超限错误分支正确填期望输出预留；错误同时说明最低输出，准入算法未改。
- #59：实际用量和请求前读数分别携带精度；圆环标记历史已变化；同步 Host 和前端
  replay、可重复执行的 UI 补丁、已提交静态资源及资源 revision。

## 验证环境与结果

当前源代码（含未提交修改）rsync 至 `WZU_Server:~/codex-build/x-harness-rs/`。
排除 .git、target、node_modules、.env 等；本机只执行格式化和 Node 测试，没有编译 Rust。

| 远程范围 | 通过 |
|---|---:|
| Host 单元 / API 集成 | 103 + 19 |
| Token | 13 |
| Core 单元 / Loop | 8 + 78 |
| Host App 单元 / 启动 / 持久化与设置 | 44 |
| OpenAI Provider 单元 / 网络恢复 / 协议 | 14 + 13 + 22 |
| 合计 | 314 |

已有 crash_matrix 中另有 1 项 ignored，本次未执行；没有将其计入通过数。
Host / Token 的 `cargo clippy --all-targets -- -D warnings` 通过。

Node 回归通过：

- `test-assistant-projection.mjs`：与 Rust 共用 wire fixture，执行实际打包的 reducer、
  finalNode 和内容 classifier；覆盖交错、Unicode、两个工具、空/纯思考/纯正文、
  完成替换、中断和刷新。将编号恢复成旧版冲突值，断言确实可观察到内容丢失。
- `test-context-accounting.mjs`：实际与估算优先级、各自精度、calibrated、迟到 usage、
  Compact、失败无 usage、模型切换、旧字段兼容、0/无容量/非法值。
- `test-context-plugin.mjs`、`test-conversation-message-edit.mjs`、`test-model-controls.mjs`。
- v1 → v2 两个 UI 补丁均可重现本次静态资源，二次执行幂等；所有已打包 JS 语法解析
  通过，插件哈希和 HTML 启动图同步。

## 未包含

不冒充真实 DeepSeek 请求验收，也未执行 Windows/macOS 原生构建或当前软件手动点验。
本次没有重启用户应用、修改对话数据、发布安装包、推送分支或关闭 Issue。
源码通过回归后仍需推送 CI 和发布，正在运行的软件不会因修改源码而自动生效。
