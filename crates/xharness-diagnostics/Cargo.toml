[package]
name = "xharness-diagnostics"
version = "0.1.0"
edition = "2021"
license = "Apache-2.0"
description = "Bounded, payload-free runtime diagnostics shared by desktop and Host"

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"
